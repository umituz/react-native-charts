/**
 * Chart Configuration Interface
 *
 * Re-export ChartConfig as interface for consistency.
 */

export type { ChartConfig, AxisConfig, LegendConfig, AnimationConfig, TooltipConfig, ChartDimensions } from '../entities/ChartConfig';
